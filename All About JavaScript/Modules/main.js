// Import the functions and rename them
import { add as sum, subtract as difference } from './math.js';

// Use the renamed imports
console.log(sum(10, 5));        // Output: 15
console.log(difference(10, 5)); // Output: 5
