let p = true;
let q = false;

console.log("AND (&&):", p && q); // false (both need to be true)
console.log("OR (||):", p || q);  // true (one can be true)
console.log("NOT (!):", !p);      // false (NOT true = false)
